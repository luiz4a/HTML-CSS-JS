// Cria um vetor com 3 notas
const notas = [5,5, 10];

// Calcula a média
const media = (notas[0] + notas[1] + notas[2]) / notas.length;

// Exibe as notas e a média
console.log("Notas:", notas);
console.log("Média:", media.toFixed(2));
